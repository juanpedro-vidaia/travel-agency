node_modules/
.next/
.env.local
next-env.d.ts

# TypeScript incremental build cache
*.tsbuildinfo

# Gestor de paquetes: este proyecto usa npm (package-lock.json)
pnpm-lock.yaml
yarn.lock

# Config local del harness de Claude Code (permisos por máquina)
.claude/settings.local.json
